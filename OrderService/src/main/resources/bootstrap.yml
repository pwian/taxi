spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order
    username: sa
    password: sa
  jpa:
    show-sql: false
  application:
    name: taxiService
  cloud:
    uri: http://config:8888
    fail-fast: true
    password: ${CONFIG_SERVICE_PASSWORD}
    username: user